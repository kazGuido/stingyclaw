# NeuTTS — TTS only. GGUF backbone via pre-built llama-cpp-python (see Dockerfile).
setuptools>=65,<82  # pkg_resources removed in 82; required by resemble-perth (watermarker) used by neutts
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
neutts>=1.1.0
# llama-cpp-python installed from CPU wheel in Dockerfile (no compile)